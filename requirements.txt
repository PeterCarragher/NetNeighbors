# Web UI (Dash + Cytoscape network visualization)
dash>=2.14.0
dash-cytoscape>=1.0.0

# Webgraph interface (handles Java bridge, graph loading, discovery)
pyccwebgraph>=0.1.0

# Data processing
pandas>=2.0.0

# Utilities (optional, for notebook usage)
tqdm>=4.0.0
psutil>=5.0.0
ipywidgets>=8.0.0
